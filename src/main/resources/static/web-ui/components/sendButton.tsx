import {Button} from "@heroui/react";

export default function SendButton() {
  return (
    <div className="flex gap-4 items-center">
      <Button size="md" type="submit">Add Command</Button>
    </div>
  );
}